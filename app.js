const demoText = `今儿咱说个事儿
说出来丢人
想起来又挺解气
上个月在布拉格看一个东亚文化展
展厅正中挂着一幅两米多高的《水浒人物长卷》
水墨压下来
每一笔都像藏着股杀气
旁边一个研究中国古典小说很多年的老教授
听说我是中国人
先指着林冲问
风雪山神庙那段
林冲杀陆谦之前
为什么偏偏先看一眼酒葫芦
我当场愣住
我知道林冲
知道风雪山神庙
可从没想过他拔刀前为什么还要看那一眼
他又指着鲁智深
三拳打死镇关西
按你们古代的规矩杀人偿命
他怎么还能大摇大摆走出城
接着又问武松
十八碗酒下肚
人都醉成那样了
为什么还能看清老虎那三招
茶都凉了
一个外国人问我中国英雄的命运
我一个也答不上来
老教授见我不说话
指着画说
你看林冲眉头的雪
鲁智深拳头的茧
宋江腰间的刀
每个人手里的兵器
脸上的皱纹
都藏着各自的前世今生
你们自己的英雄得自己知道啊
九八版《水浒》我看过无数遍
片尾长卷一展开
宋江
武松
林冲一个个从宣纸上站起来
可我从没认真想过画他们的人是谁
回国一查
越查越心寒
身边几个朋友
画都眼熟
真问戴敦邦是谁
大多摇头
更扎心的是
咱的孩子背得出漫威英雄的生日
分得清霍格沃茨四个学院
却叫不出林冲的兵器
再往下查
一个右眼已经失明
凌晨三点还在画中国英雄的老人
很多人甚至连名字都没听过
我不甘心
托朋友去了上海
在那间不足五平米
没空调的画室里见到戴老
我把布拉格那个问题端给他
林冲杀陆谦前
为什么先看一眼酒葫芦
老爷子放下笔
那壶酒是他娘子以前给他温过的
他看一眼
是想把好日子最后看一眼
酒还温着
人已经走到绝路了
我后背一麻
人家画的不是模样
是命
我又问
您画里的人怎么就能从纸上站起来
戴老说
当年九八版央视《水浒》开拍
剧组第一件事不是找演员
是登门请他出山
经费紧
他就一句
不给钱也画
此后两年闭门不出
一百八十四个人物一个个从宣纸上站起来
人物得先在心里活
到了纸上才站得住
不是画出来的
是活过来的
后来演员全是按图挑的
服装
化妆也照着这些造型去找感觉
画完
分文未取
我问
这么大的活
不要钱
您图什么
戴老摸出一方印
四个字
民间艺人
他说
一九七九年我去敦煌
看那些壁画
一千多年了
那些没有留下名字的民间画工
却在墙上留下了千年不朽的色彩
他说自己当场就落了泪
回来以后便刻了这方民间艺人
人留不留名字没那么要紧
画留下了
后面的人还能看
这就够了
二零二二年
他又把《水浒》《资本论》等珍贵画稿无偿捐了出去
到这儿
我本来还想再问
可第三个问题我没开口
戴老自己往下说了
他说早年给英文版《红楼梦》画过三十六幅插图
获奖无数
可说到这些
他却摇了摇头
那是给外国人看故事的
我要画中国人看得懂的中国味儿
后来再画《红楼梦》
气管炎犯了
就拿后背撞门框
撞完接着画
画到写《与妻书》的林觉民
他一边画一边哭
十五个月
后来右眼就看不见了
最后戴老看着桌上那些人物说
我这辈子就怕一件事
外国人拿咱们的英雄当宝贝
咱们自己拿它当旧纸
我半天没接话
他又补了一句
咱们把老祖宗的故事送进别人的博物馆
还管这叫文化交流
从上海回来
我第一件事就是想给孩子找一套戴老的画
可原作拍卖近千万买不起
学术画册厚得像砖头孩子翻不动
廉价印刷品又糊成一团
看不出水墨的筋骨
找了快一个月
我碰到这套戴敦邦四大名著人物典藏卡
三百二十四张
扑克牌大小
刚好盈握掌心
《水浒传》《三国演义》《西游记》《红楼梦》一套收齐
正面是戴老的水墨人物
林冲雪夜里紧锁的眉头
孙悟空大闹天宫时飞扬的毫毛
黛玉葬花低垂的眼睫
诸葛亮借东风羽扇轻摇的从容
画室里一笔一划磨出来的精气神
一张张收进了方寸之间
翻到背面
是问答式人物小传
鲁智深为什么叫花和尚
武松打虎前喝了多少碗酒
后来为什么又叫行者
孙悟空一个筋斗十万八千里
为什么还是翻不出如来的手掌心
一张卡就是一个人物的前世今生
我拿到那天
真蹲在那里翻了一下午
这种知识随便拎一个
饭桌上都能聊半天
孩子问起来
你能当场接上
懂行的人再问两句
你能从人物聊到故事
从故事聊到笔墨
人家一听就知道你不是附庸风雅
是真懂这里头的分量
说白了
这张卡就是给你长脸的
孩子拿着也不用硬背
等车抽一张
睡前翻两张
一张张看过去
四大名著的人物和脉络慢慢就顺下来了
碎片时间
就把文化血脉接上了
纸张做了加厚处理
圆角切边不伤手
水墨纹理也保留下来了
不是买回来落灰的藏品
是能随手拿
随手翻
随手把玩的东西
给孩子
是最生动的名著启蒙
给自己
是圆小时候集卡片的梦
送朋友
是送到心坎里的中式浪漫
厚厚四本名著
最后变成一套能揣兜里
拿手上的掌上名著博物馆
买再多乐高和进口绘本
不如给孩子留一副中国人的精神底牌
房子会折旧
车子会换代
可戴老用一生心血讲的这些中国故事
只会越来越重
戴老已经八十多岁
右眼失明
双耳几近失聪
还每天凌晨三点在画室里提笔
这套卡也是他最后一次完整画齐四大名著全部人物
官方已经说明
这一批售完以后不再复刻
卖一套就少一套
当年九八版《水浒》片尾那幅徐徐展开的水墨长卷
早就刻进我们这一代人的记忆里
现在有机会把这些人物完整拿在手里
也该给自己的孩子补上了
别等以后真有个外国人
指着林冲
武松问咱孩子一句他为什么会这样
还得让别人来给咱讲
这套戴敦邦四大名著人物典藏卡就在下面
话说回来
当年片尾长卷展开的时候
你第一个认出来的是谁
评论区聊聊`;

const searchCount = 8;
const batchSize = 4;

const phraseQueries = {
  说个事: ["说话 口播 人物", "人物 讲述"],
  说话: ["说话 口播 人物", "人物 讲话"],
  丢人: ["真人 尴尬 影视剧照", "不好意思 人物"],
  不好意思: ["真人 尴尬 影视剧照", "人物 害羞 尴尬"],
  解气: ["真人 解气 影视剧照", "痛快 人物"],
  展厅: ["艺术展厅", "展厅 看画"],
  每一笔: ["毛笔 写字 特写", "书法 笔锋"],
  杀气: ["水墨 杀气 笔锋", "凌厉 书法 笔锋"],
  写字: ["毛笔 写字 特写", "书法 笔锋"],
  听说: ["侧耳 倾听", "人物 听别人说话"],
  中国人: ["中国人 人物", "中国传统人物"],
  指着: ["人物 指着 画", "手指 指向"],
  神庙: ["神庙 建筑", "古代神庙"],
  雪山神庙: ["雪山 神庙", "风雪 山神庙"],
  拔刀: ["古代人物 拔刀", "人物带刀"],
  带刀: ["古代人物 带刀", "腰间佩刀"],
  古代: ["中国古代 人物", "古代街道"],
  走出城: ["古代城门 出城", "走出城门"],
  古代喝酒: ["古代喝酒", "古人 酒馆 喝酒"],
  喝醉: ["真人 喝醉 影视剧照", "醉酒 人物"],
  老虎: ["老虎", "猛虎 特写"],
  茶: ["一杯茶", "茶杯 热茶"],
  外国人: ["外国人 人物", "外国人 看展"],
  语塞: ["真人 语塞 影视剧照", "答不上来 人物"],
  一幅画: ["一幅画 展厅", "墙上挂画"],
  拳头上茧: ["拳头 老茧 特写", "手上老茧"],
  宋江带刀: ["宋江 带刀", "水浒 宋江 佩刀"],
  皱纹: ["老人 皱纹 特写", "脸上皱纹"],
  戴老: ["戴敦邦 老画家", "戴敦邦 画室"],
  失明: ["老人 右眼失明", "失明 老人"],
  失聪: ["老人 失聪", "老人 听不见"],
  凌晨三点: ["凌晨 画室 画画", "夜里 提笔 画画"],
  剧组: ["电视剧剧组", "古装剧 剧组"],
  演员: ["古装演员 剧照", "演员 试装"],
  登门: ["登门拜访 老画家", "拜访 艺术家"],
  经费紧: ["经费紧张 剧组", "预算紧张"],
  闭门不出: ["闭门 画画", "画家 闭门创作"],
  画工: ["古代画工 壁画", "民间画工"],
  落泪: ["老人 落泪", "人物 流泪"],
  画稿: ["中国画 画稿", "水墨画稿"],
  捐赠: ["捐赠 画稿", "艺术品 捐赠"],
  英文版: ["英文版 红楼梦 插图", "红楼梦 英文版"],
  插图: ["书籍 插图", "中国画 插图"],
  获奖: ["获奖 证书", "艺术获奖"],
  中国味儿: ["中国风 水墨", "中式美学"],
  气管炎: ["老人 生病 画画", "咳嗽 老人"],
  门框: ["门框", "靠着门框"],
  哭: ["人物 哭泣", "老人 哭"],
  旧纸: ["旧纸 特写", "泛黄旧纸"],
  博物馆: ["博物馆 展厅", "博物馆 藏品"],
  拍卖: ["艺术品 拍卖", "拍卖会"],
  画册: ["中国画 画册", "艺术画册"],
  印刷品: ["印刷品 模糊", "书籍 印刷"],
  卡片: ["人物卡片", "收藏卡片"],
  正面: ["卡片正面", "人物卡 正面"],
  背面: ["卡片背面", "问答卡片 背面"],
  问答: ["问答卡片", "人物小传 卡片"],
  饭桌: ["饭桌 聊天", "餐桌 交谈"],
  长脸: ["人物 自信 有面子", "成功 人物"],
  等车: ["等车 看卡片", "公交站 阅读"],
  睡前: ["睡前 阅读", "孩子 睡前看书"],
  碎片时间: ["碎片时间 阅读", "通勤 阅读"],
  文化血脉: ["中国传统文化", "传统文化 孩子"],
  加厚: ["厚纸卡片", "加厚卡片"],
  圆角: ["圆角卡片", "卡片圆角"],
  藏品: ["收藏品 展示", "藏品 落灰"],
  把玩: ["手拿卡片", "把玩 卡片"],
  中式浪漫: ["中式浪漫 礼物", "中国风 礼物"],
  精神底牌: ["中国文化 孩子", "传统文化 启蒙"],
  折旧: ["房子 车子", "房子 折旧"],
  限量: ["限量 收藏卡", "限量发售"],
  售完: ["售罄", "商品售完"],
  复刻: ["不再复刻 限量", "复刻 收藏品"],
  评论区: ["评论区", "手机 评论区"],
  水浒人物长卷: ["戴敦邦 水浒人物长卷", "98版水浒 片尾长卷"],
  风雪山神庙: ["林冲 风雪山神庙 剧照", "水浒传 风雪山神庙"],
  三拳打死镇关西: ["鲁智深 三拳打死镇关西", "水浒传 鲁智深 镇关西"],
  武松打虎: ["武松打虎 电视剧 剧照", "水浒传 武松打虎"],
  十八碗酒: ["武松 十八碗酒", "武松打虎 十八碗酒"],
  酒葫芦: ["林冲 酒葫芦 风雪山神庙", "水浒 林冲 酒葫芦"],
  戴敦邦四大名著人物典藏卡: ["戴敦邦 四大名著人物典藏卡", "戴敦邦 人物典藏卡"],
  戴敦邦: ["戴敦邦 水浒 人物画", "戴敦邦 四大名著"],
  九八版水浒: ["98版水浒 剧照", "央视水浒 1998 剧照"],
  央视水浒: ["央视水浒 1998 剧照", "98版水浒 片尾长卷"],
  片尾长卷: ["98版水浒 片尾长卷", "水浒传 片尾 长卷"],
  林冲: ["林冲 水浒传 剧照", "戴敦邦 林冲"],
  鲁智深: ["鲁智深 水浒传 剧照", "戴敦邦 鲁智深"],
  武松: ["武松 水浒传 剧照", "戴敦邦 武松"],
  宋江: ["宋江 水浒传 剧照", "戴敦邦 宋江"],
  陆谦: ["林冲 陆谦 风雪山神庙"],
  镇关西: ["鲁智深 镇关西"],
  水浒传: ["水浒传 电视剧 剧照", "水浒传 人物画"],
  水浒: ["水浒传 电视剧 剧照", "水浒 人物画 戴敦邦"],
  西游记: ["西游记 电视剧 剧照", "西游记 人物图"],
  孙悟空: ["孙悟空 大闹天宫", "西游记 孙悟空 剧照"],
  大闹天宫: ["孙悟空 大闹天宫 动画", "西游记 大闹天宫"],
  红楼梦: ["红楼梦 电视剧 剧照", "红楼梦 人物画"],
  黛玉葬花: ["黛玉葬花 电视剧 剧照", "红楼梦 黛玉葬花"],
  三国演义: ["三国演义 电视剧 剧照", "三国演义 人物图"],
  诸葛亮借东风: ["诸葛亮 借东风 剧照", "三国演义 诸葛亮 借东风"],
  四大名著: ["四大名著 人物图", "四大名著 人物卡"],
  资本论: ["资本论 插图", "戴敦邦 资本论"],
  与妻书: ["与妻书 林觉民 插画", "林觉民 与妻书"],
  林觉民: ["林觉民 与妻书 插画", "林觉民 人物"],
  民间艺人: ["民间艺人 印章", "中国民间艺人"],
  敦煌: ["敦煌壁画", "敦煌 莫高窟 壁画"],
  壁画: ["敦煌壁画", "中国古代壁画"],
  布拉格: ["布拉格 东亚文化展", "布拉格 展览"],
  东亚文化展: ["东亚文化展 展厅", "布拉格 东亚文化展"],
  上海: ["上海 老画室", "上海 艺术家 画室"],
  画室: ["中国画家 画室", "水墨画 画室"],
  老教授: ["老教授 展厅 中国画", "教授 看画 展览"],
  中国古典小说: ["中国古典小说 四大名著", "四大名著"],
  中国英雄: ["中国英雄 水浒人物", "水浒英雄 人物画"],
  漫威: ["漫威英雄", "Marvel heroes"],
  霍格沃茨: ["霍格沃茨 四个学院", "Harry Potter Hogwarts houses"],
  兵器: ["水浒人物 兵器", "中国古代兵器"],
  宣纸: ["水墨 宣纸 长卷", "中国画 宣纸"],
  水墨: ["水墨人物画", "中国水墨画"],
  长卷: ["水墨长卷", "中国画 长卷"],
  服装: ["水浒传 服装 造型", "古装 服装 化妆"],
  化妆: ["水浒传 化妆 造型", "古装剧 化妆"],
  人物小传: ["四大名著 人物小传", "人物卡 背面 问答"],
  花和尚: ["鲁智深 花和尚", "水浒传 花和尚"],
  行者: ["武松 行者", "水浒传 武松 行者"],
  如来: ["孙悟空 如来 手掌心", "西游记 如来佛祖"],
  筋斗云: ["孙悟空 筋斗云", "西游记 筋斗云"],
  人物典藏卡: ["戴敦邦 四大名著人物典藏卡", "四大名著 人物典藏卡"],
  扑克牌大小: ["四大名著 人物卡", "典藏卡 扑克牌大小"],
  水墨纹理: ["水墨纹理 特写", "中国画 水墨纹理"],
  名著启蒙: ["四大名著 儿童启蒙", "名著启蒙 卡片"],
  掌上名著博物馆: ["掌上名著博物馆", "四大名著人物卡"],
  乐高: ["乐高 积木", "孩子 乐高"],
  进口绘本: ["儿童 进口绘本", "绘本 儿童"],
};

const sceneRules = [
  [/林冲.*酒葫芦|酒葫芦.*林冲/, ["林冲 风雪山神庙 酒葫芦", "98版水浒 林冲 酒葫芦"]],
  [/林冲.*陆谦|陆谦.*林冲/, ["林冲杀陆谦 风雪山神庙", "水浒传 林冲 陆谦"]],
  [/鲁智深.*镇关西|镇关西.*鲁智深/, ["鲁智深 三拳打死镇关西", "水浒传 鲁智深 镇关西"]],
  [/武松.*老虎|老虎.*武松/, ["武松打虎 电视剧 剧照", "武松打虎 十八碗酒"]],
  [/九八版|98版|央视.*水浒|水浒.*片尾/, ["98版水浒 片尾长卷", "央视水浒 1998 剧照"]],
  [/孙悟空.*大闹天宫|大闹天宫.*孙悟空/, ["孙悟空 大闹天宫", "西游记 孙悟空 大闹天宫"]],
  [/黛玉.*葬花|葬花.*黛玉/, ["黛玉葬花 电视剧 剧照", "红楼梦 黛玉葬花"]],
  [/诸葛亮.*借东风|借东风.*诸葛亮/, ["诸葛亮 借东风 剧照", "三国演义 诸葛亮 借东风"]],
  [/戴敦邦.*典藏卡|典藏卡.*戴敦邦/, ["戴敦邦 四大名著人物典藏卡", "戴敦邦 人物典藏卡"]],
];

const visualRules = [
  [/布拉格.*东亚文化展|东亚文化展.*布拉格/, ["布拉格 东亚文化展", "布拉格", "东亚文化展 展厅"]],
  [/展厅.*水浒人物长卷|水浒人物长卷.*展厅/, ["展厅 水浒人物长卷", "水浒人物长卷", "艺术展厅"]],
  [/林冲.*陆谦|陆谦.*林冲/, ["林冲 陆谦", "林冲杀陆谦 风雪山神庙"]],
  [/林冲.*酒葫芦|酒葫芦.*林冲/, ["林冲 酒葫芦", "酒葫芦"]],
  [/鲁智深.*镇关西|镇关西.*鲁智深|三拳打死镇关西/, ["三拳打死镇关西", "鲁智深 镇关西"]],
  [/武松.*老虎|老虎.*武松|武松打虎/, ["武松打虎", "武松 老虎"]],
  [/宋江.*腰间.*刀|宋江.*刀|腰间.*刀/, ["宋江带刀", "宋江 腰刀", "古代人物 带刀"]],
  [/鲁智深.*拳头.*茧|拳头.*茧/, ["拳头上茧", "鲁智深 拳头"]],
  [/林冲.*眉头.*雪|眉头.*雪/, ["林冲 雪", "林冲"]],
  [/今儿咱说|咱说|说个事/, ["说话 口播 人物", "人物 讲述"]],
  [/丢人|不好意思|尴尬/, ["真人 尴尬 影视剧照", "不好意思 人物"]],
  [/解气|痛快|出气/, ["真人 解气 影视剧照", "痛快 人物"]],
  [/东亚文化展/, ["东亚文化展 展厅", "文化展览"]],
  [/布拉格/, ["布拉格", "布拉格 展览"]],
  [/展厅/, ["艺术展厅", "展厅 看画"]],
  [/水墨/, ["水墨 宣纸", "中国水墨画"]],
  [/每一笔|杀气|笔锋/, ["毛笔 写字 杀气", "书法 笔锋"]],
  [/老教授|教授/, ["老教授", "教授 看画 展览"]],
  [/听说|听别人说/, ["侧耳 倾听", "人物 听别人说话"]],
  [/中国人/, ["中国人 人物", "中国传统人物"]],
  [/指着.*林冲|林冲.*问/, ["指着林冲", "林冲"]],
  [/指着.*画|画说/, ["指着一幅画", "墙上挂画"]],
  [/风雪山神庙|雪山神庙/, ["雪山神庙", "风雪山神庙"]],
  [/酒葫芦/, ["酒葫芦", "林冲 酒葫芦"]],
  [/愣住|答不上来|语塞|不说话/, ["真人 语塞 影视剧照", "愣住 人物"]],
  [/拔刀|刀前/, ["古代人物 拔刀", "人物带刀"]],
  [/鲁智深/, ["鲁智深", "鲁智深 水浒传 剧照"]],
  [/林冲/, ["林冲", "林冲 水浒传 剧照"]],
  [/武松/, ["武松", "武松 水浒传 剧照"]],
  [/宋江/, ["宋江", "宋江 水浒传 剧照"]],
  [/古代.*规矩|杀人偿命|古代/, ["中国古代 人物", "古代街道"]],
  [/走出城|出城/, ["古代城门 出城", "走出城门"]],
  [/十八碗酒|下肚/, ["古代喝酒", "武松 十八碗酒"]],
  [/醉/, ["真人 喝醉 影视剧照", "醉酒 人物"]],
  [/老虎/, ["老虎", "猛虎 特写"]],
  [/茶.*凉|茶/, ["一杯茶", "茶杯 热茶"]],
  [/外国人/, ["外国人 人物", "外国人 看展"]],
  [/一幅画|挂画/, ["一幅画 展厅", "墙上挂画"]],
  [/兵器/, ["中国古代兵器", "水浒人物 兵器"]],
  [/皱纹/, ["老人 皱纹 特写", "脸上皱纹"]],
  [/前世今生|命运/, ["人物命运", "人物小传"]],
  [/站起来|活过来|活过来的/, ["水墨人物 站起来", "人物画"]],
  [/心寒|扎心|不甘心|后背一麻/, ["真人 震惊 影视剧照", "人物 心寒"]],
  [/摇头/, ["人物 摇头", "摇头 真人"]],
  [/孩子|儿童/, ["孩子 阅读", "儿童 名著启蒙"]],
  [/失明|看不见|右眼/, ["老人 右眼失明", "失明 老人"]],
  [/失聪|听不见|双耳/, ["老人 失聪", "老人 听不见"]],
  [/凌晨三点|夜里|提笔/, ["凌晨 画室 画画", "夜里 提笔 画画"]],
  [/上海/, ["上海", "上海 老画室"]],
  [/画室|不足五平米|没空调/, ["中国画家 画室", "老画家 画室"]],
  [/戴老|戴敦邦/, ["戴敦邦 老画家", "戴敦邦 人物画"]],
  [/娘子|温过|酒还温着/, ["古代 温酒", "古代夫妻"]],
  [/绝路/, ["古代人物 绝路", "绝望 人物"]],
  [/剧组|开拍/, ["电视剧剧组", "古装剧 剧组"]],
  [/演员|按图挑/, ["古装演员 剧照", "演员 试装"]],
  [/服装|化妆|造型/, ["古装 服装 化妆", "水浒传 服装 造型"]],
  [/印|一方印|四个字/, ["中国印章", "民间艺人 印章"]],
  [/敦煌|壁画|画工/, ["敦煌壁画", "民间画工"]],
  [/落泪|哭/, ["人物 流泪", "老人 落泪"]],
  [/捐|画稿/, ["捐赠 画稿", "中国画 画稿"]],
  [/英文版|插图|获奖/, ["英文版 红楼梦 插图", "书籍 插图"]],
  [/中国味儿|中国味/, ["中国风 水墨", "中式美学"]],
  [/气管炎|撞门框/, ["老人 生病 画画", "靠着门框"]],
  [/旧纸/, ["旧纸 特写", "泛黄旧纸"]],
  [/博物馆/, ["博物馆 展厅", "博物馆 藏品"]],
  [/拍卖|近千万/, ["艺术品 拍卖", "拍卖会"]],
  [/画册|印刷品|糊成一团/, ["中国画 画册", "印刷品 模糊"]],
  [/典藏卡|人物卡|扑克牌|掌心|卡片/, ["戴敦邦 四大名著人物典藏卡", "人物卡片"]],
  [/正面|背面|问答式|人物小传/, ["人物卡 正面 背面", "问答卡片"]],
  [/饭桌|聊半天/, ["饭桌 聊天", "餐桌 交谈"]],
  [/附庸风雅|真懂|分量/, ["中国画 鉴赏", "看画 交流"]],
  [/长脸|有面子/, ["人物 自信 有面子", "成功 人物"]],
  [/等车|睡前|碎片时间/, ["碎片时间 阅读", "孩子 睡前看书"]],
  [/文化血脉|传统文化/, ["中国传统文化", "传统文化 孩子"]],
  [/加厚|圆角|切边|不伤手/, ["厚纸圆角卡片", "圆角卡片"]],
  [/水墨纹理/, ["水墨纹理 特写", "中国画 水墨纹理"]],
  [/落灰|藏品/, ["收藏品 展示", "藏品 落灰"]],
  [/随手拿|随手翻|把玩/, ["手拿卡片", "把玩 卡片"]],
  [/名著启蒙/, ["四大名著 儿童启蒙", "名著启蒙 卡片"]],
  [/中式浪漫/, ["中式浪漫 礼物", "中国风 礼物"]],
  [/掌上名著博物馆/, ["掌上名著博物馆", "四大名著人物卡"]],
  [/精神底牌/, ["中国文化 孩子", "传统文化 启蒙"]],
  [/房子|车子|折旧|换代/, ["房子 车子", "房子 折旧"]],
  [/限量|售完|不再复刻|卖一套少一套/, ["限量 收藏卡", "商品售罄"]],
  [/记忆里|这一代人/, ["童年记忆", "怀旧 电视剧"]],
  [/评论区|聊聊/, ["评论区", "手机 评论区"]],
];

const phraseOrder = Object.keys(phraseQueries).sort((a, b) => b.length - a.length);
const state = {
  tasks: [],
  basket: [],
  pickSeq: 0,
  ratio: "9 / 16",
};

const $ = (selector) => document.querySelector(selector);
const els = {
  textInput: $("#textInput"),
  charCount: $("#charCount"),
  buildButton: $("#buildButton"),
  clearButton: $("#clearButton"),
  demoButton: $("#demoButton"),
  manualInput: $("#manualInput"),
  manualButton: $("#manualButton"),
  taskCount: $("#taskCount"),
  taskTags: $("#taskTags"),
  imageCount: $("#imageCount"),
  collapseAllButton: $("#collapseAllButton"),
  emptyResults: $("#emptyResults"),
  resultList: $("#resultList"),
  basketCount: $("#basketCount"),
  emptyBasket: $("#emptyBasket"),
  basketList: $("#basketList"),
  autoPickButton: $("#autoPickButton"),
  exportButton: $("#exportButton"),
  ratioButtons: document.querySelectorAll("[data-ratio]"),
  toast: $("#toast"),
};

function splitSegments(text) {
  const lines = text.split(/\n+/).map((line) => line.trim()).filter(Boolean);
  if (lines.length > 1) return lines.flatMap(splitLongSegment);
  return splitLongSegment(text.trim());
}

function splitLongSegment(segment) {
  if (!segment) return [];
  if (segment.length <= 90) return [segment];

  const pieces = segment.split(/(?<=[。！？!?；;])/).map((piece) => piece.trim()).filter(Boolean);
  if (pieces.length <= 1) return [segment];

  const segments = [];
  let bucket = "";
  pieces.forEach((piece) => {
    if (bucket && `${bucket}${piece}`.length > 90) {
      segments.push(bucket);
      bucket = piece;
    } else {
      bucket += piece;
    }
  });
  if (bucket) segments.push(bucket);
  return segments;
}

function extractTerms(segment) {
  const terms = [];
  const add = (value) => {
    const term = value.trim().replace(/[，。！？、；：“”"']/g, "");
    if (term.length < 2 || terms.some((item) => item === term || item.includes(term))) return;
    terms.filter((item) => term.includes(item)).forEach((item) => terms.splice(terms.indexOf(item), 1));
    terms.push(term);
  };

  [...segment.matchAll(/《([^》]{2,24})》/g)].forEach((match) => add(match[1]));
  visualRules.forEach((rule) => {
    if (rule[0].test(segment)) rule[1].forEach(add);
  });
  phraseOrder.forEach((phrase) => {
    if (segment.includes(phrase)) add(phrase);
  });
  return terms.slice(0, 8);
}

function buildQueries(segment, terms, lastQuery) {
  const queries = [];
  const add = (query) => {
    const clean = query.trim().replace(/\s+/g, " ");
    if (clean.length >= 2 && !queries.includes(clean)) queries.push(clean);
  };

  visualRules.forEach((rule) => {
    if (rule[0].test(segment)) rule[1].forEach(add);
  });
  sceneRules.forEach((rule) => {
    if (rule[0].test(segment)) rule[1].forEach(add);
  });
  terms.forEach((term) => (phraseQueries[term] || [term]).forEach(add));
  if (!queries.length) add(fallbackQuery(segment));
  return queries.slice(0, 5);
}

function fallbackQuery(segment) {
  if (/丢人|解气|愣住|心寒|扎心|不甘心|后背一麻|答不上来/.test(segment)) return "真人 影视 情绪 特写";
  if (/孩子|女儿|儿童/.test(segment)) return "孩子 阅读 四大名著";
  if (/外国人|教授|展厅/.test(segment)) return "外国人 展厅 看中国画";
  if (/画|水墨|笔墨/.test(segment)) return "中国水墨人物画";
  return segment.replace(/[^\u4e00-\u9fffA-Za-z0-9]+/g, " ").trim().slice(0, 24) || "相关画面";
}

function buildTasks(text) {
  let lastQuery = "";
  return splitSegments(text).map((segment, index) => {
    const terms = extractTerms(segment);
    const queries = buildQueries(segment, terms, lastQuery);
    lastQuery = queries[0] || lastQuery;
    return {
      id: `S${String(index + 1).padStart(2, "0")}`,
      order: index + 1,
      segment,
      terms,
      queries,
      query: queries[0],
      page: 0,
      loading: false,
      expanded: false,
      items: [],
      error: "",
    };
  });
}

async function buildAndSearch() {
  const text = els.textInput.value.trim();
  if (!text) {
    showToast("先粘贴文案。");
    return;
  }
  state.tasks = buildTasks(text);
  renderAll();
  showToast(`已生成 ${state.tasks.length} 个搜索任务，开始后台搜图。`);
  await searchTasksInBatches();
}

async function searchTasksInBatches() {
  for (let index = 0; index < state.tasks.length; index += batchSize) {
    await Promise.all(state.tasks.slice(index, index + batchSize).map((task) => searchTask(task)));
  }
  updateImageCount();
}

async function searchTask(task, append = false) {
  task.loading = true;
  task.error = "";
  renderAll();
  try {
    const page = append ? task.page + 1 : 0;
    const response = await fetch(`/api/search?q=${encodeURIComponent(task.query)}&page=${page}&count=${searchCount}`);
    if (!response.ok) throw new Error("搜索失败");
    const data = await response.json();
    const seen = new Set(task.items.map((item) => item.image || item.thumb));
    const incoming = (data.items || []).filter((item) => !seen.has(item.image || item.thumb));
    task.items = append ? [...task.items, ...incoming] : incoming;
    task.page = page;
  } catch {
    task.error = "搜索接口暂时没返回结果，可以点百度/必应/抖音继续搜。";
  } finally {
    task.loading = false;
    renderAll();
  }
}

function renderAll() {
  renderTaskTags();
  renderResults();
  renderBasket();
  updateImageCount();
}

function renderTaskTags() {
  els.taskCount.textContent = `${state.tasks.length} 段`;
  const unique = [...new Set(state.tasks.flatMap((task) => task.queries))];
  els.taskTags.innerHTML = unique.slice(0, 80).map((query) => `<span class="task-chip">${escapeHtml(query)}</span>`).join("");
}

function renderResults() {
  els.emptyResults.hidden = state.tasks.length > 0;
  els.resultList.innerHTML = state.tasks
    .map((task, index) => {
      const count = task.loading ? "搜索中" : `${task.items.length} 张`;
      const baidu = `https://image.baidu.com/search/index?tn=baiduimage&word=${encodeURIComponent(task.query)}`;
      const bing = `https://www.bing.com/images/search?q=${encodeURIComponent(task.query)}`;
      const douyin = `https://www.douyin.com/search/${encodeURIComponent(task.query)}`;
      return `
        <section class="task-row ${task.expanded ? "is-expanded" : ""}" data-index="${index}">
          <div class="row-head">
            <button class="row-toggle" data-toggle="${index}" aria-expanded="${task.expanded}">
              <span class="arrow">${task.expanded ? "⌄" : "›"}</span>
              <span class="task-id">${task.id}</span>
              <span class="query-title">${escapeHtml(task.query)}</span>
              <span class="badge">${count}</span>
            </button>
            <div class="row-links">
              <a href="${baidu}" target="_blank" rel="noreferrer">百度</a>
              <a href="${bing}" target="_blank" rel="noreferrer">必应</a>
              <a href="${douyin}" target="_blank" rel="noreferrer">抖音</a>
            </div>
          </div>
          ${task.expanded ? `<p class="excerpt">${escapeHtml(task.segment)}</p>${renderQueryChips(task, index)}${renderTaskBody(task, index)}` : ""}
        </section>
      `;
    })
    .join("");
}

function renderQueryChips(task, index) {
  if (task.queries.length < 2) return "";
  return `
    <div class="query-chips">
      ${task.queries
        .map(
          (query) =>
            `<button class="query-chip ${query === task.query ? "is-active" : ""}" data-query-index="${index}" data-query="${escapeHtml(query)}">${escapeHtml(query)}</button>`,
        )
        .join("")}
    </div>
  `;
}

function renderTaskBody(task, index) {
  if (task.loading && !task.items.length) return `<div class="result-body"><div class="notice">正在搜索 ${escapeHtml(task.query)}...</div></div>`;
  if (task.error && !task.items.length) return `<div class="result-body"><div class="notice">${escapeHtml(task.error)}</div></div>`;
  if (!task.items.length) return `<div class="result-body"><div class="notice">暂时没有结果，可以点右侧百度/必应/抖音继续搜。</div></div>`;

  return `
    <div class="result-body">
      <div class="body-toolbar">
        <button class="button button-light button-small" data-collapse="${index}">收起本段</button>
      </div>
      <div class="image-grid">
        ${task.items.map((item, itemIndex) => renderImageCard(item, index, itemIndex)).join("")}
      </div>
      <button class="button button-light load-more" data-more="${index}">${task.loading ? "正在加载..." : "加载更多图片"}</button>
    </div>
  `;
}

function renderImageCard(item, taskIndex, itemIndex) {
  return `
    <article class="image-card">
      <img src="${item.thumb}" alt="${escapeHtml(item.title)}" loading="lazy" referrerpolicy="no-referrer" onerror="this.closest('.image-card').remove()" />
      <div class="image-info">
        <strong class="image-title">${escapeHtml(item.title || "图片结果")}</strong>
        <span class="image-meta">${escapeHtml(item.source || "图片搜索")}</span>
        <div class="image-actions">
          <button class="button button-primary" data-add-task="${taskIndex}" data-add-item="${itemIndex}">添加</button>
          <a class="button button-light" href="${item.page || item.image}" target="_blank" rel="noreferrer">来源</a>
        </div>
      </div>
    </article>
  `;
}

function toggleTask(index) {
  const task = state.tasks[index];
  if (!task) return;
  task.expanded = !task.expanded;
  renderResults();
}

function collapseTask(index) {
  const task = state.tasks[index];
  if (!task) return;
  task.expanded = false;
  renderResults();
}

function collapseAllTasks() {
  state.tasks.forEach((task) => {
    task.expanded = false;
  });
  renderResults();
}

function setTaskQuery(index, query) {
  const task = state.tasks[index];
  if (!task || task.query === query) return;
  task.query = query;
  task.page = 0;
  task.items = [];
  task.error = "";
  task.expanded = true;
  searchTask(task);
}

function addManualTask() {
  const query = els.manualInput.value.trim();
  if (!query) return;
  els.manualInput.value = "";
  const task = {
    id: `M${String(state.tasks.filter((item) => item.id.startsWith("M")).length + 1).padStart(2, "0")}`,
    order: state.tasks.length + 1,
    segment: "手动添加",
    terms: [query],
    queries: [query],
    query,
    page: 0,
    loading: false,
    expanded: true,
    items: [],
    error: "",
  };
  state.tasks.unshift(task);
  renderAll();
  searchTask(task);
}

function findItem(taskIndex, itemIndex) {
  const task = state.tasks[taskIndex];
  const item = task?.items[itemIndex];
  if (!task || !item) return null;
  const itemKey = item.image || item.thumb || `${task.id}-${itemIndex}`;
  return {
    ...item,
    itemKey,
    taskId: task.id,
    order: task.order || taskIndex + 1,
    query: task.query,
    segment: task.segment,
  };
}

function addToBasket(taskIndex, itemIndex) {
  const item = findItem(taskIndex, itemIndex);
  if (!item) return;
  const exists = state.basket.some((saved) => saved.taskId === item.taskId && saved.itemKey === item.itemKey);
  if (exists) {
    showToast("这张图已经添加过。");
    return;
  }
  state.basket.push({ ...item, selectedAt: ++state.pickSeq });
  renderBasket();
  showToast("已添加到素材篮。");
}

function orderedBasketItems() {
  const slotByTask = {};
  return [...state.basket]
    .sort((a, b) => (a.order - b.order) || (a.selectedAt - b.selectedAt))
    .map((item) => {
      const key = item.taskId || item.order || "M";
      slotByTask[key] = (slotByTask[key] || 0) + 1;
      return { ...item, slot: slotByTask[key] };
    });
}

function autoPickFirstImages() {
  let added = 0;
  state.tasks.forEach((task, index) => {
    if (!task.items.length || state.basket.some((item) => item.taskId === task.id)) return;
    state.basket.push({
      ...task.items[0],
      itemKey: task.items[0].image || task.items[0].thumb || `${task.id}-0`,
      taskId: task.id,
      order: task.order || index + 1,
      query: task.query,
      segment: task.segment,
      selectedAt: ++state.pickSeq,
    });
    added += 1;
  });
  renderBasket();
  showToast(added ? `已按顺序添加 ${added} 张首图。` : "没有可添加的新图片。");
}

function renderBasket() {
  els.emptyBasket.hidden = state.basket.length > 0;
  els.basketCount.textContent = `${state.basket.length} 张`;
  els.basketList.innerHTML = orderedBasketItems()
    .map(
      (item, index) => `
        <article class="basket-item">
          <img src="${item.thumb}" alt="${escapeHtml(item.title)}" referrerpolicy="no-referrer" />
          <div class="basket-copy">
            <strong>${String(index + 1).padStart(3, "0")} ${escapeHtml(item.taskId || "")}-${item.slot} ${escapeHtml(item.title || "图片结果")}</strong>
            <span>${escapeHtml(item.query)} · ${escapeHtml(item.source)}</span>
            <button class="remove-button" data-delete="${escapeHtml(`${item.taskId}-${item.selectedAt}`)}">移除</button>
          </div>
        </article>
      `,
    )
    .join("");
}

function updateImageCount() {
  const count = state.tasks.reduce((sum, task) => sum + task.items.length, 0);
  els.imageCount.textContent = state.tasks.length ? `${count} 张图片` : "等待生成";
}

async function exportBasket() {
  if (!state.basket.length) autoPickFirstImages();
  const items = orderedBasketItems();
  if (!items.length) {
    showToast("先生成并搜到图片，再导出。");
    return;
  }
  els.exportButton.disabled = true;
  showToast(`正在按顺序打包 ${items.length} 张图片...`);
  try {
    const response = await fetch("/api/export", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ items }),
    });
    if (!response.ok) throw new Error("导出失败");
    const blob = await response.blob();
    const url = URL.createObjectURL(blob);
    const link = document.createElement("a");
    link.href = url;
    link.download = "聚海剪映导入图片.zip";
    link.click();
    URL.revokeObjectURL(url);
    showToast("已导出，解压后按文件名顺序导入剪映。");
  } catch {
    showToast("导出失败，可以少选几张后再试。");
  } finally {
    els.exportButton.disabled = false;
  }
}

function clearAll() {
  els.textInput.value = "";
  els.charCount.textContent = "0 字";
  state.tasks = [];
  state.basket = [];
  state.pickSeq = 0;
  renderAll();
}

function setRatio(ratio) {
  state.ratio = ratio;
  document.documentElement.style.setProperty("--thumb-ratio", ratio);
  els.ratioButtons.forEach((button) => button.classList.toggle("is-active", button.dataset.ratio === ratio));
}

function escapeHtml(value) {
  return String(value || "").replace(/[&<>"']/g, (character) => {
    const entities = { "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#039;" };
    return entities[character];
  });
}

let toastTimer;
function showToast(message) {
  els.toast.textContent = message;
  els.toast.classList.add("is-visible");
  clearTimeout(toastTimer);
  toastTimer = setTimeout(() => els.toast.classList.remove("is-visible"), 2300);
}

els.textInput.addEventListener("input", () => {
  els.charCount.textContent = `${els.textInput.value.length} 字`;
});
els.buildButton.addEventListener("click", buildAndSearch);
els.clearButton.addEventListener("click", clearAll);
els.demoButton.addEventListener("click", () => {
  els.textInput.value = demoText;
  els.charCount.textContent = `${demoText.length} 字`;
  buildAndSearch();
});
els.manualButton.addEventListener("click", addManualTask);
els.manualInput.addEventListener("keydown", (event) => {
  if (event.key === "Enter") addManualTask();
});
els.resultList.addEventListener("click", (event) => {
  const toggleIndex = event.target.closest("[data-toggle]")?.dataset.toggle;
  const addButton = event.target.closest("[data-add-task][data-add-item]");
  const collapseIndex = event.target.closest("[data-collapse]")?.dataset.collapse;
  const moreIndex = event.target.dataset.more;
  const queryIndex = event.target.dataset.queryIndex;

  if (toggleIndex !== undefined) toggleTask(Number(toggleIndex));
  if (queryIndex !== undefined) setTaskQuery(Number(queryIndex), event.target.dataset.query);
  if (collapseIndex !== undefined) collapseTask(Number(collapseIndex));
  if (addButton) addToBasket(Number(addButton.dataset.addTask), Number(addButton.dataset.addItem));
  if (moreIndex !== undefined) searchTask(state.tasks[Number(moreIndex)], true);
});
els.basketList.addEventListener("click", (event) => {
  const id = event.target.dataset.delete;
  if (!id) return;
  state.basket = state.basket.filter((item) => `${item.taskId}-${item.selectedAt}` !== id);
  renderBasket();
});
els.autoPickButton.addEventListener("click", autoPickFirstImages);
els.exportButton.addEventListener("click", exportBasket);
els.collapseAllButton.addEventListener("click", collapseAllTasks);
els.ratioButtons.forEach((button) => {
  button.addEventListener("click", () => setRatio(button.dataset.ratio));
});
setRatio(state.ratio);
